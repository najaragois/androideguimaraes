<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.unifor.najaragois.telamenucurso">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".TelaMenuCursos"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".TelaListaCursos"
            android:label="@string/title_activity_tela_lista_cursos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TelaDetalhesCurso"
            android:label="@string/title_activity_tela_detalhes_curso"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>
